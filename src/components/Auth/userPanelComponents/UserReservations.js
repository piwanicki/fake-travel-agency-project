import React from 'react';
import styled from 'styled-components';

const UserReservationsDiv = styled.div`
  padding: 2em;
`;

const UserReservations = props => {
  return (
    <UserReservationsDiv>

    </UserReservationsDiv>
  )
}

export default UserReservations;